// IMPORTANT: This is a placeholder. After running `amplify push`, Amplify will generate a real `aws-exports.js` here.
// Keep this file so the app imports correctly before backend is provisioned.
const awsmobile = {
  aws_project_region: 'YOUR_REGION',
  // ... other configuration will be generated
};
export default awsmobile;
